'use strict';

// Generated by CoffeeScript 2.3.2
var hash, normalize, setup;

hash = {};

normalize = function (ord) {
  var arr;
  arr = ord.split('');
  arr.sort();
  return arr.join('');
};

setup = function () {
  var div, i, inp, key, len, ord, ref;
  ref = ordlista.split(' ');
  for (i = 0, len = ref.length; i < len; i++) {
    ord = ref[i];
    key = normalize(ord);
    if (hash[key]) {
      hash[key].push(ord);
    } else {
      hash[key] = [ord];
    }
  }
  inp = createInput('');
  inp.input(function () {
    var unscrambled;
    key = normalize(this.value());
    unscrambled = hash[key];
    return div.html(unscrambled ? unscrambled : '');
  });
  return div = createDiv('Mata in 4-9 bokstäver. T ex dunh');
};
//# sourceMappingURL=sketch.js.map
