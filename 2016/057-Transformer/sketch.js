// Generated by CoffeeScript 1.11.1
var Transformer, dump, g, mousePressed, players, setup, xdraw;

g = 0;

players = [];

Transformer = (function() {
  function Transformer() {
    this.x = 0;
    this.y = 0;
    this.a = 0;
    this.s = 1;
    this.stack = [];
  }

  Transformer.prototype.push = function() {
    this.stack.push([this.x, this.y, this.a, this.s]);
    return push();
  };

  Transformer.prototype.pop = function() {
    var ref;
    ref = this.stack.pop(), this.x = ref[0], this.y = ref[1], this.a = ref[2], this.s = ref[3];
    return pop();
  };

  Transformer.prototype.rotate = function(d) {
    this.a += d;
    return rotate(d);
  };

  Transformer.prototype.scale = function(ds) {
    this.s *= ds;
    return scale(ds);
  };

  Transformer.prototype.translate = function(dx, dy) {
    this.x += this.s * dx * cos(this.a) - this.s * dy * sin(this.a);
    this.y += this.s * dy * cos(this.a) + this.s * dx * sin(this.a);
    return translate(dx, dy);
  };

  return Transformer;

})();

dump = function(txt) {
  return console.log([txt, this.x, this.y]);
};

setup = function() {
  createCanvas(600, 300);
  textAlign(CENTER, CENTER);
  rectMode(CENTER);
  g = new Transformer();
  players.push(new Player("WASD", color(255, 0, 0)));
  players.push(new Player("&%('", color(0, 255, 0)));
  return xdraw();
};

xdraw = function() {
  var i, j, len, player;
  g.push();
  g.translate(width / 2, height / 2);
  for (i = j = 0, len = players.length; j < len; i = ++j) {
    player = players[i];
    g.push();
    if (i === 0) {
      g.translate(-width / 4, 0);
      g.rotate(90);
    }
    if (i === 1) {
      g.translate(width / 4, 0);
      g.rotate(-90);
      g.scale(1);
    }
    player.draw();
    g.pop();
  }
  return g.pop();
};

mousePressed = function() {
  var j, len, player, results;
  console.log([mouseX, mouseY]);
  results = [];
  for (j = 0, len = players.length; j < len; j++) {
    player = players[j];
    results.push(player.mousePressed());
  }
  return results;
};
