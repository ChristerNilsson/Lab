// Generated by CoffeeScript 1.11.1
var Player;

Player = (function() {
  function Player(keys, col) {
    this.keys = keys;
    this.col = col;
    this.w = 30;
    this.h = 60;
    this.buttons = [];
    this.buttons.push(new Button(this, -10, -10, 7.5, 15, "3"));
    this.buttons.push(new Button(this, 10, -10, 7.5, 15, "2"));
    this.buttons.push(new Button(this, 0, -10, 7.5, 15, "undo"));
    this.buttons.push(new Button(this, -10, 10, 7.5, 15, "/2"));
    this.buttons.push(new Button(this, 0, 10, 7.5, 15, "+2"));
    this.buttons.push(new Button(this, 10, 10, 7.5, 15, "*2"));
  }

  Player.prototype.draw = function() {
    var button, i, len, ref, results;
    fill(this.col);
    rect(0, 0, width * this.w / 60 - 2, height * this.h / 60 - 2);
    ref = this.buttons;
    results = [];
    for (i = 0, len = ref.length; i < len; i++) {
      button = ref[i];
      results.push(button.draw());
    }
    return results;
  };

  Player.prototype.mousePressed = function() {
    var button, i, len, ref, results;
    ref = this.buttons;
    results = [];
    for (i = 0, len = ref.length; i < len; i++) {
      button = ref[i];
      results.push(button.mousePressed());
    }
    return results;
  };

  return Player;

})();
