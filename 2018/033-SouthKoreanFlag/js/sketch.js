"use strict";

// Generated by CoffeeScript 2.0.3
// https://en.wikipedia.org/wiki/Flag_of_South_Korea#/media/File:Flag_of_South_Korea_(construction_sheet).svg
var ram, setup, streck, vinkel, xdraw, yinyang;

vinkel = null;

setup = function setup() {
  createCanvas(1200, 800);
  angleMode(DEGREES);
  rectMode(CENTER);
  vinkel = atan2(2, 3);
  return xdraw();
};

ram = function ram() {
  fc(1);
  sw(1 / 300);
  sc(0);
  return rect(0, 0, 3, 2);
};

yinyang = function yinyang() {
  sc();
  fc(1, 0, 0);
  arc(0, 0, 1, 1, 180, 0);
  fc(0, 0, 1);
  arc(0, 0, 1, 1, 0, 180);
  fc(1, 0, 0);
  circle(-1 / 4, 0, 1 / 4);
  fc(0, 0, 1);
  return circle(1 / 4, 0, 1 / 4);
};

streck = function streck(pattern, offset, black, white) {
  var i, len, p;
  push();
  translate(offset, 0);
  for (i = 0, len = pattern.length; i < len; i++) {
    p = pattern[i];
    translate(black / 2, 0);
    fc(0);
    sc(1);
    rect(0, 0, 1 / 12, 1 / 2);
    if (p === 0) {
      fc(1);
      sc(1);
      rect(0, 0, 1 / 12, 1 / 24);
    }
    translate(black / 2, 0);
    translate(white, 0);
  }
  return pop();
};

xdraw = function xdraw() {
  bg(0.5);
  translate(width / 2, height / 2);
  scale(300);
  ram();
  rotate(vinkel);
  yinyang();
  streck([1, 1, 1], -1 / 2 - 1 / 4, -1 / 12, -1 / 24);
  streck([0, 0, 0], 1 / 2 + 1 / 4, 1 / 12, 1 / 24);
  rotate(-2 * vinkel);
  streck([1, 0, 1], -1 / 2 - 1 / 4, -1 / 12, -1 / 24);
  return streck([0, 1, 0], 1 / 2 + 1 / 4, 1 / 12, 1 / 24);
};
//# sourceMappingURL=sketch.js.map
