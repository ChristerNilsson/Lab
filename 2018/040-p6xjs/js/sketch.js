"use strict";

// Generated by CoffeeScript 2.0.3
var draw, message, mouseMoved, mousePressed, moved, setup;

message = "";

moved = function moved(m) {
  return this.strokeWeight = this.contains(m) ? 3 : 1;
};

setup = function setup() {
  var alfabet, c, digit, i, j, k, len, len1, letter, numbers, results, x, y;
  createCanvas(600, 600);
  angleMode(DEGREES);
  p6.circle(300, 300, 200).fill("#ff0");
  shapes.letters = p6.group(300, 300);
  shapes.digits = p6.group(300, 300);
  alfabet = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';
  for (i = j = 0, len = alfabet.length; j < len; i = ++j) {
    letter = alfabet[i];
    x = 200 * cos(i * 360 / alfabet.length);
    y = 200 * sin(i * 360 / alfabet.length);
    c = p6.circle(x, y, 30, shapes.letters);
    c.fill("#f00");
    c.text(letter);
    c.moved = moved;
    c.pressed = function () {
      return message += this.txt;
    };
  }
  numbers = '0123456789';
  results = [];
  for (i = k = 0, len1 = numbers.length; k < len1; i = ++k) {
    digit = numbers[i];
    x = 260 * cos(i * 360 / numbers.length);
    y = 260 * sin(i * 360 / numbers.length);
    c = p6.circle(x, y, 30, shapes.digits);
    c.fill("#0f0");
    c.text(digit);
    c.moved = moved;
    results.push(c.pressed = function () {
      return message = this.txt;
    });
  }
  return results;
};

draw = function draw() {
  var child, j, k, len, len1, ref, ref1;
  bg(0.5);
  stage.draw();
  shapes.letters.rotation += 0.1;
  ref = shapes.letters.children;
  for (j = 0, len = ref.length; j < len; j++) {
    child = ref[j];
    child.rotation -= 2.9;
  }
  shapes.digits.rotation -= 0.1;
  ref1 = shapes.digits.children;
  for (k = 0, len1 = ref1.length; k < len1; k++) {
    child = ref1[k];
    child.rotation += 3.1;
  }
  textSize(100);
  textAlign(CENTER, CENTER);
  return text(message, 300, 300);
};

mouseMoved = function mouseMoved() {
  return stage.mouseMoved();
};

mousePressed = function mousePressed() {
  return stage.mousePressed();
};
//# sourceMappingURL=sketch.js.map
