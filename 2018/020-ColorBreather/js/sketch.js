"use strict";

// Generated by CoffeeScript 2.0.3
var COLORS, changeCorners, current, draw, keys, setup;

current = 0;

COLORS = {};

keys = null;

changeCorners = function changeCorners() {
  current = 0;
  return keys.push(keys.shift());
};

setup = function setup() {
  COLORS.black = color(0, 0, 0);
  COLORS.blue = color(0, 0, 255);
  COLORS.green = color(0, 255, 0);
  COLORS.cyan = color(0, 255, 255);
  COLORS.red = color(255, 0, 0);
  COLORS.magenta = color(255, 0, 255);
  COLORS.yellow = color(255, 255, 0);
  COLORS.white = color(255, 255, 255);
  keys = _.shuffle(_.keys(COLORS));
  createCanvas(windowWidth, windowHeight);
  textSize(0.4 * height);
  return textAlign(CENTER, CENTER);
};

draw = function draw() {
  var key1, key2;
  key1 = keys[0];
  key2 = keys[1];
  background(lerpColor(COLORS[key1], COLORS[key2], current));
  if (current > 1) {
    changeCorners();
  } else {
    current += 0.001;
  }
  fc(0);
  text(key1, 0.5 * width, 0.3 * height);
  translate(0.5 * width, 0.7 * height);
  rd(180);
  fc(1);
  return text(key1, 0, 0);
};
//# sourceMappingURL=sketch.js.map
