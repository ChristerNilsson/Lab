'use strict';

// Generated by CoffeeScript 2.0.3
var bearing, draw, result, setup;

bearing = 0;

result = 'no lock';

setup = function setup() {
  screen.lockOrientationUniversal = screen.lockOrientation || screen.mozLockOrientation || screen.msLockOrientation;
  if (screen.lockOrientationUniversal) {
    result = screen.lockOrientationUniversal('portrait');
  }
  createCanvas(windowWidth, windowHeight);
  window.addEventListener("deviceorientation", function (event) {
    if (event.alpha) {
      return bearing = Math.round(event.alpha);
    }
  });
  textSize(100);
  return textAlign(CENTER, CENTER);
};

draw = function draw() {
  bg(1);
  text(result, width / 2, height / 4);
  return text(bearing, width / 2, height / 2);
};
//# sourceMappingURL=sketch.js.map
