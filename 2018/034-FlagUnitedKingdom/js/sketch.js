"use strict";

// Generated by CoffeeScript 2.0.3
// https://en.wikipedia.org/wiki/Union_Jack#/media/File:United_Kingdom_Flag_Specifications.svg

// 25:2:6:2:25 = 60
// 10:2:6:2:10 = 30
var arm, diagonal, diagonal2, setup;

setup = function setup() {
  var i, j, k, len, len1, ref, ref1, results, vinkel;
  createCanvas(1200, 600);
  angleMode(DEGREES);
  vinkel = atan2(30, 60);
  translate(width / 2, height / 2);
  scale(20);
  sc();
  bg(0, 0, 1);
  diagonal2(vinkel);
  diagonal2(-vinkel);
  ref = range(2);
  // white
  for (j = 0, len = ref.length; j < len; j++) {
    i = ref[j];
    arm(1, 1, 1, 10, 60);
    rotate(90);
  }
  ref1 = range(2);
  // red
  results = [];
  for (k = 0, len1 = ref1.length; k < len1; k++) {
    i = ref1[k];
    arm(1, 0, 0, 6, 60);
    results.push(rotate(90));
  }
  return results;
};

diagonal2 = function diagonal2(vinkel) {
  rotate(vinkel);
  diagonal();
  rotate(180);
  diagonal();
  return rotate(-vinkel);
};

diagonal = function diagonal() {
  rectMode(CORNER);
  fc(1);
  rect(0, -3, 40, 6);
  fc(1, 0, 0);
  return rect(0, -2, 40, 2);
};

arm = function arm(r, g, b, w, h) {
  rectMode(CENTER);
  fc(r, g, b);
  return rect(0, 0, w, h);
};
//# sourceMappingURL=sketch.js.map
