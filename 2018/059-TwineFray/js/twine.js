'use strict';

// Generated by CoffeeScript 2.0.3
var Black,
    Green,
    Red,
    Yellow,
    button,
    data,
    display,
    draw,
    enemy,
    events,
    goto,
    mousePressed,
    person,
    rand,
    setup,
    t,
    indexOf = [].indexOf;

person = {};

enemy = {};

events = {};

data = []; // texts and buttons

Black = function Black(txt) {
  return t(txt, 0, 0, 0);
};

Green = function Green(txt) {
  return t(txt, 0, 1, 0);
};

Red = function Red(txt) {
  return t(txt, 1, 0, 0);
};

Yellow = function Yellow(txt) {
  return t(txt, 1, 1, 0);
};

t = function t(txt, r, g, b) {
  return data.push({ txt: txt, r: r, g: g, b: b });
};

button = function button(name) {
  var txt = arguments.length > 1 && arguments[1] !== undefined ? arguments[1] : '';

  return data.push({ name: name, txt: txt });
};

display = function display(txt) {
  return events[txt]();
};

goto = function goto(loc) {
  return display(loc);
};

rand = function rand(a, b) {
  return int(a + random(b - a));
};

setup = function setup() {
  createCanvas(600, 600);
  textSize(16);
  return events.Start();
};

draw = function draw() {
  var b, g, i, item, j, len, r, results, s, txt;
  bg(0.75);
  results = [];
  for (i = j = 0, len = data.length; j < len; i = ++j) {
    item = data[i];
    if ('name' in item) {
      // button
      s = item.txt;
      if (s === '') {
        s = item.name;
      }
      fc(0, 0, 1);
      results.push(text(s, 20, 20 + 20 * i)); // text
    } else {
      var _item = item;
      txt = _item.txt;
      r = _item.r;
      g = _item.g;
      b = _item.b;

      fc(r, g, b);
      results.push(text(txt, 20, 20 + 20 * i));
    }
  }
  return results;
};

mousePressed = function mousePressed() {
  var i, item, j, len;
  for (i = j = 0, len = data.length; j < len; i = ++j) {
    item = data[i];
    if (indexOf.call(_.keys(item), 'name') >= 0) {
      if (20 * i < mouseY && mouseY < 20 + 20 * i) {
        data = [];
        events[item.name]();
      }
    }
  }
  return print(JSON.stringify(person));
};
//# sourceMappingURL=twine.js.map
