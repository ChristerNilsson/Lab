'use strict';

// Generated by CoffeeScript 2.3.2
var bets, draw, _gcd, i, mousePressed, pretty, setup, total;

bets = function () {
  var j, len, ref, results;
  ref = range(10);
  results = [];
  for (j = 0, len = ref.length; j < len; j++) {
    i = ref[j];
    results.push(0);
  }
  return results;
}();

total = 0;

setup = function setup() {
  createCanvas(200, 200);
  textAlign(CENTER, CENTER);
  return textSize(20);
};

_gcd = function gcd(x, y) {
  if (y === 0) {
    return x;
  } else {
    return _gcd(y, x % y);
  }
};

pretty = function pretty(a, b) {
  var g;
  if (b === 0) {
    return '';
  }
  g = _gcd(a, b);
  a = Math.floor(a / g);
  b = Math.floor(b / g);
  if (b === 1) {
    return a;
  } else {
    return a + '/' + b;
  }
};

draw = function draw() {
  var j, len, name, ref, results;
  bg(0.5);
  ref = 'Adam Bertil Cesar David Erik Filip Gustav Helge Ivar Johan'.split(' ');
  results = [];
  for (i = j = 0, len = ref.length; j < len; i = ++j) {
    name = ref[i];
    text(name, 50, 10 + i * 20);
    if (bets[i] > 0) {
      text(bets[i], 100, 10 + i * 20);
    }
    results.push(text('' + pretty(total, bets[i]), 150, 10 + i * 20));
  }
  return results;
};

mousePressed = function mousePressed() {
  i = Math.floor(mouseY / 20);
  bets[i]++;
  return total++;
};
//# sourceMappingURL=sketch.js.map
