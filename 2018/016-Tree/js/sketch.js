"use strict";

// Generated by CoffeeScript 2.0.3
var CRLF, _generateTree, meny;

CRLF = "\n";

meny = null;

window.onload = function () {

  //world = generateTree 3
  return meny = new Menu(world);
};

_generateTree = function generateTree(level) {
  var hsh, i, key, len, ref;
  if (level === 0) {
    return {};
  }
  hsh = {};
  ref = 'ABCDEFGHIJKLMNOPQRSTUVWXYZ';
  for (i = 0, len = ref.length; i < len; i++) {
    key = ref[i];
    hsh[key + Math.random(10)] = _generateTree(level - 1);
  }
  return hsh;
};

//console.log generateTree 3
//# sourceMappingURL=sketch.js.map
