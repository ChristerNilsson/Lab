"use strict";

// Generated by CoffeeScript 2.0.3
var draw, setup;

setup = function setup() {
  var readDeviceOrientation;
  createCanvas(windowWidth, windowHeight);
  readDeviceOrientation = function readDeviceOrientation() {
    if (90 === Math.abs(window.orientation)) {
      return text("L " + width + ' ' + height, width / 2, height / 2);
    } else {
      return text("P " + width + ' ' + height, width / 2, height / 2);
    }
  };
  window.onorientationchange = readDeviceOrientation;
  return readDeviceOrientation();
};

draw = function draw() {};
//# sourceMappingURL=sketch.js.map
