"use strict";

// Generated by CoffeeScript 2.0.3
var canvas, draw, setup;

canvas = null;

setup = function setup() {
  var readDeviceOrientation;
  canvas = createCanvas(windowWidth, windowHeight);
  textAlign(CENTER, CENTER);
  readDeviceOrientation = function readDeviceOrientation() {
    var x, y;
    if (90 === Math.abs(window.orientation)) {
      resizeCanvas(windowWidth / 2, windowHeight / 2);
      x = width; // (windowWidth - width) / 2;
      y = 0; //(windowHeight - height) / 2;
      canvas.position(x, y);
      //text "L " + width+' '+height,width/2,height/2
      return text("L " + width + ' ' + height, 100, 100);
    } else {
      resizeCanvas(windowWidth / 2, windowHeight / 2);
      x = width; // (windowWidth - width) / 2;
      y = 0; //(windowHeight - height) / 2;
      canvas.position(x, y);
      //text "P " + width+' '+height,width/2,height/2
      return text("P " + width + ' ' + height, 100, 100);
    }
  };
  window.onorientationchange = readDeviceOrientation;
  return readDeviceOrientation();
};

draw = function draw() {
  return bg(0.5);
};
//# sourceMappingURL=sketch.js.map
